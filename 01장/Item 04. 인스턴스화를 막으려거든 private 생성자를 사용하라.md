# 인스턴스화를 막으려거든 private 생성자를 사용하라

- Utility성격의 클래스는 static메서드만 가지고 있으며, 인스턴스 만드는 것을 권장하지 않는다. (java.lang.Math, java.util.Arrays..)
- 추상클래스는 상속으로 하위클래스를 만들어 하위클래스의 인스턴스를 만들 때 super()로 생성자 호출이 가능해 인스턴스화를 막기에 충분하지 않다.
- 생성자를 private으로 만들면 인스턴스 생성을 막을 수 있다.
- 직관적인 사용을 주석을 달아 문서화를 해 두자.
